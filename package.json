{
  "name": "json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port 10000 --middlewares cors.js"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
